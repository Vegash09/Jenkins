bundle:
  name: Deal Share Project

include:
  - "resources/*.yml"
  - "resources/*.yaml"

targets:
  production:
    mode: production
    default: true
    workspace:
      host: https://adb-7405617499680447.7.azuredatabricks.net
      root_path: /Workspace/Shared/${bundle.target}