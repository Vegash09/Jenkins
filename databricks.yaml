bundle:
  name: Deal Share Project
  include:
    - "pipelines/*.yml"
    - "pipelines/*.yaml"
  exclude:
    - "Jenkinsfile"
    - "backup/**"
    - "databricks.yml"
    - "README.md"

targets:
  production:
    mode: production
    default: true
    workspace:
      host: https://dbc-a8b738a8-498f.cloud.databricks.com/
      root_path: /Workspace/Shared/${bundle.target}

