bundle:
  name: Jenkins

# Target environment
targets:
  production:
    mode: production
    default: true
    workspace:
      host: https://adb-7405617499680447.7.azuredatabricks.net/
      # This will deploy to /Workspace/Shared/Jenkins/production
      root_path: /Workspace/Shared/${bundle.name}/${bundle.target}